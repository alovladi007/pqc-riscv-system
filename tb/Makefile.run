# Per-test driver. Invoked by the top-level Makefile with MODULE, TOPLEVEL,
# and VERILOG_SOURCES set. This file delegates to cocotb's bundled makefile.
include $(shell cocotb-config --makefiles)/Makefile.sim
